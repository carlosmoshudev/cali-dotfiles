# Load every alias fragment in lexical order.
# Keep the files small and grouped by concern so the structure stays readable.

alias_dir="${HOME}/.dotfiles/zsh/aliases"

if [[ -d "$alias_dir" ]]; then
  for alias_file in "$alias_dir"/*.zsh(N); do
    source "$alias_file"
  done
fi
