diff -ruN /mnt/data/zsh-original-layout/zsh/.zshrc /mnt/data/zsh-reviewed/zsh/.zshrc
--- /mnt/data/zsh-original-layout/zsh/.zshrc	2026-07-29 11:32:58.718280621 +0000
+++ /mnt/data/zsh-reviewed/zsh/.zshrc	2026-07-29 11:32:58.706280620 +0000
@@ -1,15 +1,22 @@
+# Powerlevel10k instant prompt must stay close to the top.
 if [[ -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
   source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
 fi
 
-[[ $- != *i* ]] && return
+[[ $- == *i* ]] || return
 
-unalias_safe_file="$HOME/.dotfiles/zsh/init/00-unalias-safe.zsh"
-if [[ -r "$unalias_safe_file" ]]; then
-  source "$unalias_safe_file"
-fi
+_load_zsh_init() {
+  local init_dir="$HOME/.dotfiles/zsh/init"
+  local unalias_file="$init_dir/00-unalias-safe.zsh"
+  local init_file
+
+  [[ -r "$unalias_file" ]] && source "$unalias_file"
+
+  for init_file in "$init_dir"/*.zsh(N); do
+    [[ "$init_file" == "$unalias_file" ]] && continue
+    source "$init_file"
+  done
+}
 
-for init_file in "$HOME/.dotfiles/zsh/init"/*.zsh(N); do
-  [[ $init_file == "$unalias_safe_file" ]] && continue
-  source "$init_file"
-done
+_load_zsh_init
+unfunction _load_zsh_init
diff -ruN /mnt/data/zsh-original-layout/zsh/.zshrc_aliases /mnt/data/zsh-reviewed/zsh/.zshrc_aliases
--- /mnt/data/zsh-original-layout/zsh/.zshrc_aliases	2026-07-29 11:32:58.718280621 +0000
+++ /mnt/data/zsh-reviewed/zsh/.zshrc_aliases	2026-07-29 11:32:58.706280620 +0000
@@ -1,10 +1,14 @@
 # Load every alias fragment in lexical order.
-# Keep the files small and grouped by concern so the structure stays readable.
+_load_zsh_aliases() {
+  local alias_dir="$HOME/.dotfiles/zsh/aliases"
+  local alias_file
 
-alias_dir="${HOME}/.dotfiles/zsh/aliases"
+  [[ -d "$alias_dir" ]] || return 0
 
-if [[ -d "$alias_dir" ]]; then
   for alias_file in "$alias_dir"/*.zsh(N); do
     source "$alias_file"
   done
-fi
+}
+
+_load_zsh_aliases
+unfunction _load_zsh_aliases
diff -ruN /mnt/data/zsh-original-layout/zsh/aliases/00-shell.zsh /mnt/data/zsh-reviewed/zsh/aliases/00-shell.zsh
--- /mnt/data/zsh-original-layout/zsh/aliases/00-shell.zsh	2026-07-29 11:32:58.722280621 +0000
+++ /mnt/data/zsh-reviewed/zsh/aliases/00-shell.zsh	2026-07-29 11:32:58.714280621 +0000
@@ -1,12 +1,20 @@
-# Small shell helpers that should be available everywhere.
+# Universal interactive-shell helpers.
+
+if (( $+commands[batcat] )); then
+  alias cat='batcat --paging=never --style=plain'
+  alias bat='batcat'
+elif (( $+commands[bat] )); then
+  alias cat='bat --paging=never --style=plain'
+else
+  alias cat='/bin/cat'
+fi
 
-alias cat='batcat --paging=never'
 alias tcat='/bin/cat'
-alias bat='batcat'
 alias grep='grep --color=auto'
-
 alias cls='clear'
-alias reload='source ~/.zshrc'
+alias reload='source "$HOME/.zshrc"'
 alias moshu-ipscan="$HOME/scripts/moshu-ipscan"
 
-alias please='sudo $(fc -ln -1)'
+# The Oh My Zsh sudo plugin already supports prefixing the current command
+# with sudo (press Esc twice). A history-reexecution alias is intentionally
+# avoided because it does not preserve every command safely.
diff -ruN /mnt/data/zsh-original-layout/zsh/aliases/01-system.zsh /mnt/data/zsh-reviewed/zsh/aliases/01-system.zsh
--- /mnt/data/zsh-original-layout/zsh/aliases/01-system.zsh	2026-07-29 11:32:58.722280621 +0000
+++ /mnt/data/zsh-reviewed/zsh/aliases/01-system.zsh	2026-07-29 11:32:58.714280621 +0000
@@ -1,11 +1,34 @@
-# System and network helpers.
+# System, network, maintenance, disk and journal helpers.
 
 alias df='df -h'
 alias du='du -h'
+alias dfh='df -hT -x tmpfs -x devtmpfs -x squashfs'
+alias upgradable='apt list --upgradable'
 
-alias update='sudo apt-get update && sudo apt-get upgrade -y'
-alias upgradable='apt-get list --upgradable'
+update() {
+  sudo apt update && sudo apt upgrade "$@"
+}
 
-alias ports='ss -tulpen'
-alias myip='curl ifconfig.me'
-alias weather='curl wttr.in'
+# Show first-level disk usage. Unlike an alias containing a pipe, this function
+# correctly accepts a path: du1 /srv
+# -x keeps the scan on the same filesystem.
+du1() {
+  local root="${1:-.}"
+  command du -xhd1 -- "$root" 2>/dev/null | sort -h
+}
+
+ports() {
+  sudo ss -tulpne "$@"
+}
+
+myip() {
+  curl -fsS --max-time 8 https://api.ipify.org && print
+}
+
+weather() {
+  local location="${1:-A_Coruna}"
+  curl -fsS --max-time 15 "https://wttr.in/${location}?lang=es"
+}
+
+alias jerr='journalctl -p err..alert -b'
+alias jf='journalctl -f'
diff -ruN /mnt/data/zsh-original-layout/zsh/aliases/10-navigation.zsh /mnt/data/zsh-reviewed/zsh/aliases/10-navigation.zsh
--- /mnt/data/zsh-original-layout/zsh/aliases/10-navigation.zsh	2026-07-29 11:32:58.722280621 +0000
+++ /mnt/data/zsh-reviewed/zsh/aliases/10-navigation.zsh	2026-07-29 11:32:58.714280621 +0000
@@ -5,9 +5,20 @@
 alias ....='cd ../../.. && ll'
 alias .....='cd ../../../.. && ll'
 
-alias composefiles='cd /srv/containers/pro && clear && ll'
-alias srv='cd /srv && clear && ll'
-alias server='cd /srv && clear && ll'
-alias comics='cd /srv/media/comics && clear && ll'
-alias home='cd ~ && clear && ll'
-alias media='cd /srv/media && clear && ll'
+_zjump() {
+  local base="$1"
+  shift
+  local destination="$base"
+
+  [[ $# -gt 0 ]] && destination="$base/$1"
+  cd -- "$destination" || return
+  clear
+  ll
+}
+
+composefiles() { _zjump "${STACKS_DIR:-/srv/containers/pro}" "$@"; }
+srv()          { _zjump /srv "$@"; }
+server()       { srv "$@"; }
+comics()       { _zjump "${COMICS_WORKSPACE:-/srv/media/comics}" "$@"; }
+home()         { _zjump "$HOME" "$@"; }
+media()        { _zjump "${MEDIA:-/srv/media}" "$@"; }
diff -ruN /mnt/data/zsh-original-layout/zsh/aliases/11-listing.zsh /mnt/data/zsh-reviewed/zsh/aliases/11-listing.zsh
--- /mnt/data/zsh-original-layout/zsh/aliases/11-listing.zsh	2026-07-29 11:32:58.722280621 +0000
+++ /mnt/data/zsh-reviewed/zsh/aliases/11-listing.zsh	2026-07-29 11:32:58.714280621 +0000
@@ -1,4 +1,4 @@
-# Directory listing presets built on top of lsd.
+# Directory listing presets built only on top of lsd.
 
 alias ls='lsd'
 alias l='lsd -l'
@@ -26,74 +26,93 @@
 alias lperm='lsd -lA --permission octal'
 alias linode='lsd -lA --blocks inode,permission,links,user,group,size,date,name'
 alias llinks='lsd -lA --blocks links,permission,user,group,size,date,name'
-
 alias lbytes='lsd -lA --size bytes'
 alias ldirsize='lsd -lA --total-size --sizesort'
 alias labsdate='lsd -lA --date "+%Y-%m-%d %H:%M:%S"'
-
 alias lplain='lsd -lA --icon never --color never'
 
-lstree() {
-  local depth="${1:-2}"
-  local root="${2:-.}"
+_lsd_tree_args() {
+  REPLY_DEPTH=2
+  REPLY_ROOT=.
+
+  if [[ "${1:-}" == <-> ]]; then
+    REPLY_DEPTH="$1"
+    shift
+  fi
 
-  lsd --tree --depth "$depth" -- "$root"
+  [[ $# -gt 0 ]] && REPLY_ROOT="$1"
 }
 
-ltd() {
-  local depth="${1:-3}"
-  local root="${2:-.}"
+lstree() {
+  _lsd_tree_args "$@"
+  lsd --tree --depth "$REPLY_DEPTH" -- "$REPLY_ROOT"
+}
 
-  lsd --tree --directory-only --depth "$depth" -- "$root"
+ltd() {
+  _lsd_tree_args "$@"
+  local version
+  version="$(lsd --version 2>/dev/null | awk 'NR == 1 { print $2 }')"
+
+  autoload -Uz is-at-least
+  if [[ -n "$version" ]] && is-at-least 1.2.0 "$version"; then
+    lsd --tree --directory-only --depth "$REPLY_DEPTH" -- "$REPLY_ROOT"
+  elif (( $+commands[tree] )); then
+    print -u2 -- "lsd $version does not support --directory-only with --depth; using tree."
+    command tree -d -L "$REPLY_DEPTH" -- "$REPLY_ROOT"
+  else
+    print -u2 -- "lsd $version does not support --directory-only with --depth; using find."
+    command find "$REPLY_ROOT" -maxdepth "$REPLY_DEPTH" -type d -print
+  fi
 }
 
 lstack() {
-  local root="${1:-${STACKS_DIR:-$HOME/docker}}"
-
+  local root="${1:-${STACKS_DIR:-/srv/containers/pro}}"
   lsd -lA --tree --depth 2 -- "$root"
 }
 
 lmedia() {
-  local root="${1:-${MEDIA:-/data}}"
-
+  local root="${1:-${MEDIA:-/srv/media}}"
   lsd -lA -- "$root"
 }
 
 llogs() {
   local root="${1:-/var/log}"
-
   lsd -lA --timesort --date relative -- "$root"
 }
 
 lcompose() {
-  local root="${1:-${STACKS_DIR:-$HOME/docker}}"
+  local root="${1:-${STACKS_DIR:-/srv/containers/pro}}"
 
   find "$root" -maxdepth 4 -type f \
-    \( -name 'compose.yml' \
-       -o -name 'compose.yaml' \
-       -o -name 'docker-compose.yml' \
-       -o -name 'docker-compose.yaml' \) \
+    \( -name compose.yml \
+       -o -name compose.yaml \
+       -o -name docker-compose.yml \
+       -o -name docker-compose.yaml \) \
     -print0 2>/dev/null |
     xargs -0 -r lsd -l --date relative --
 }
 
 lbroken() {
   local root="${1:-.}"
-
   find "$root" -xdev -xtype l -print0 2>/dev/null |
     xargs -0 -r lsd -ld --
 }
 
 lempty() {
   local root="${1:-.}"
-
   find "$root" -xdev -empty -print0 2>/dev/null |
     xargs -0 -r lsd -ld --date relative --
 }
 
 lchanged() {
-  local days="${1:-1}"
-  local root="${2:-.}"
+  local days=1
+  local root=.
+
+  if [[ "${1:-}" == <-> ]]; then
+    days="$1"
+    shift
+  fi
+  [[ $# -gt 0 ]] && root="$1"
 
   find "$root" -xdev -type f -mtime "-$days" -print0 2>/dev/null |
     xargs -0 -r lsd -ld --timesort --date relative --
@@ -101,21 +120,6 @@
 
 lworld() {
   local root="${1:-.}"
-
-  find "$root" -xdev \
-    \( -type f -o -type d \) \
-    -perm -0002 -print0 2>/dev/null |
+  find "$root" -xdev \( -type f -o -type d \) -perm -0002 -print0 2>/dev/null |
     xargs -0 -r lsd -ld --permission octal --
 }
-
-alias dfh='df -hT -x tmpfs -x devtmpfs -x squashfs'
-alias du1='du -xhd1 2>/dev/null | sort -h'
-alias dps='docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"'
-alias dcps='docker compose ps'
-alias dcu='docker compose up -d'
-alias dcd='docker compose down'
-alias dcl='docker compose logs -f --tail=200'
-alias ddf='docker system df -v'
-alias dtop='docker stats --no-stream'
-alias jerr='journalctl -p err..alert -b'
-alias jf='journalctl -f'
diff -ruN /mnt/data/zsh-original-layout/zsh/aliases/12-comics.zsh /mnt/data/zsh-reviewed/zsh/aliases/12-comics.zsh
--- /mnt/data/zsh-original-layout/zsh/aliases/12-comics.zsh	2026-07-29 11:32:58.722280621 +0000
+++ /mnt/data/zsh-reviewed/zsh/aliases/12-comics.zsh	2026-07-29 11:32:58.714280621 +0000
@@ -1,64 +1,45 @@
-# Comics workspace helpers.
-#
-# This file stays silent if the comics workspace is not available on the host.
-# That lets the same dotfiles repo live on machines that do not mount the
-# comics tree or its tools directory.
-
-comic_alias_source=${(%):-%N}
-comic_alias_dir=${comic_alias_source:A:h}
-comic_alias_root=${comic_alias_dir:A:h}
-
-comic_workspace=""
-
-if [[ -n ${COMICS_WORKSPACE:-} && -d ${COMICS_WORKSPACE:-} && -d "$COMICS_WORKSPACE/tools" ]]; then
-  comic_workspace=$COMICS_WORKSPACE
-elif [[ -n ${COMICS_ROOT:-} && -d ${COMICS_ROOT:-} && -d "$COMICS_ROOT/tools" ]]; then
-  comic_workspace=$COMICS_ROOT
+# Optional comics workspace helpers.
+
+_comics_workspace=""
+_alias_source=${(%):-%N}
+_alias_dir=${_alias_source:A:h}
+_alias_root=${_alias_dir:A:h}
+
+if [[ -n ${COMICS_WORKSPACE:-} && -d "$COMICS_WORKSPACE/tools" ]]; then
+  _comics_workspace="$COMICS_WORKSPACE"
+elif [[ -n ${COMICS_ROOT:-} && -d "$COMICS_ROOT/tools" ]]; then
+  _comics_workspace="$COMICS_ROOT"
 elif [[ -d /srv/media/comics/tools ]]; then
-  comic_workspace=/srv/media/comics
-elif [[ -d "$comic_alias_root/tools" ]]; then
-  comic_workspace=$comic_alias_root
-elif [[ -d "$comic_alias_root/../tools" ]]; then
-  comic_workspace=${comic_alias_root:A:h}
+  _comics_workspace=/srv/media/comics
+elif [[ -d "$_alias_root/tools" ]]; then
+  _comics_workspace="$_alias_root"
+elif [[ -d "$_alias_root/../tools" ]]; then
+  _comics_workspace=${_alias_root:A:h}
 fi
 
-if [[ -z $comic_workspace ]]; then
-  return 0
+if [[ -n "$_comics_workspace" ]]; then
+  export COMICS_WORKSPACE="$_comics_workspace"
+  export COMICS_TOOLS_DIR="$COMICS_WORKSPACE/tools"
+
+  _comic_tool() {
+    local script="$1"
+    shift
+
+    if [[ ! -f "$COMICS_TOOLS_DIR/$script" ]]; then
+      print -u2 -- "Comic tool not found: $COMICS_TOOLS_DIR/$script"
+      return 127
+    fi
+
+    bash "$COMICS_TOOLS_DIR/$script" "$@"
+  }
+
+  cbr_inspect()  { _comic_tool cbr_inspect.sh "$@"; }
+  cbr_export()   { _comic_tool cbr_export.sh "$@"; }
+  cbr_fake()     { _comic_tool cbr_fake.sh "$@"; }
+  cbz_verify()   { _comic_tool cbz_verify.sh "$@"; }
+  cbz_pack()     { _comic_tool cbz_pack.sh "$@"; }
+  cbz_pack_all() { _comic_tool cbz_pack_all.sh "$@"; }
+  cbz_flatten()  { _comic_tool cbz_flatten.sh "$@"; }
 fi
 
-comic_tools_dir="$comic_workspace/tools"
-
-if [[ ! -f "$comic_tools_dir/cbz_pack_all.sh" ]]; then
-  return 0
-fi
-
-export COMICS_WORKSPACE="$comic_workspace"
-export COMICS_TOOLS_DIR="$comic_tools_dir"
-
-cbr_inspect() {
-  bash "$COMICS_TOOLS_DIR/cbr_inspect.sh" "$@"
-}
-
-cbr_export() {
-  bash "$COMICS_TOOLS_DIR/cbr_export.sh" "$@"
-}
-
-cbr_fake() {
-  bash "$COMICS_TOOLS_DIR/cbr_fake.sh" "$@"
-}
-
-cbz_verify() {
-  bash "$COMICS_TOOLS_DIR/cbz_verify.sh" "$@"
-}
-
-cbz_pack() {
-  bash "$COMICS_TOOLS_DIR/cbz_pack.sh" "$@"
-}
-
-cbz_pack_all() {
-  bash "$COMICS_TOOLS_DIR/cbz_pack_all.sh" "$@"
-}
-
-cbz_flatten() {
-  bash "$COMICS_TOOLS_DIR/cbz_flatten.sh" "$@"
-}
+unset _comics_workspace _alias_source _alias_dir _alias_root
diff -ruN /mnt/data/zsh-original-layout/zsh/aliases/20-docker-compose.zsh /mnt/data/zsh-reviewed/zsh/aliases/20-docker-compose.zsh
--- /mnt/data/zsh-original-layout/zsh/aliases/20-docker-compose.zsh	2026-07-29 11:32:58.722280621 +0000
+++ /mnt/data/zsh-reviewed/zsh/aliases/20-docker-compose.zsh	2026-07-29 11:32:58.714280621 +0000
@@ -1,15 +1,34 @@
-# Docker Compose shortcuts.
+# Docker and Docker Compose helpers.
 
+alias dps='docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"'
+alias dcps='docker compose ps'
 alias dcu='docker compose up -d'
 alias docker-up='docker compose up -d'
 alias dcd='docker compose down'
 alias docker-down='docker compose down'
-alias dcdu='docker compose down && docker compose up -d'
-alias docker-restart='docker compose down && docker compose up -d'
-alias dcl='docker compose logs -f --tail=100'
-alias docker-logs='docker compose logs -f --tail=100'
-alias dupg='docker compose down && docker compose pull && docker compose up -d && docker image prune -a -f'
-alias docker-upgrade='docker compose down && docker compose pull && docker compose up -d && docker image prune -a -f'
-alias dlogs='docker compose logs -f --tail=100'
+alias dcl='docker compose logs -f --tail=200'
+alias dlogs='docker compose logs -f --tail=200'
+alias docker-logs='docker compose logs -f --tail=200'
 alias dexec='docker compose exec'
 alias docker-exec='docker compose exec'
+alias ddf='docker system df -v'
+alias dtop='docker stats --no-stream'
+
+dcrestart() {
+  docker compose restart "$@"
+}
+alias docker-restart='dcrestart'
+
+# Pull first to avoid needless downtime. Do not run `docker image prune -a`
+# automatically: that removes every unused image on the host, including useful
+# rollback images from unrelated stacks.
+dcupgrade() {
+  docker compose pull "$@" &&
+    docker compose up -d --remove-orphans "$@"
+}
+alias docker-upgrade='dcupgrade'
+
+# Conservative cleanup; dangling images only.
+dprune() {
+  docker image prune -f
+}
diff -ruN /mnt/data/zsh-original-layout/zsh/aliases/21-docker-gluetun.zsh /mnt/data/zsh-reviewed/zsh/aliases/21-docker-gluetun.zsh
--- /mnt/data/zsh-original-layout/zsh/aliases/21-docker-gluetun.zsh	2026-07-29 11:32:58.722280621 +0000
+++ /mnt/data/zsh-reviewed/zsh/aliases/21-docker-gluetun.zsh	2026-07-29 11:32:58.718280621 +0000
@@ -1,7 +1,18 @@
-# Gluetun port helper.
-
-unalias ffport 2>/dev/null
+# Gluetun forwarded-port helper.
 
 ffport() {
-  docker exec -it gluetun sh -lc 'cat /tmp/gluetun/forwarded_port'
+  local container="${GLUETUN_CONTAINER:-gluetun}"
+  local port
+
+  port="$(docker exec "$container" cat /tmp/gluetun/forwarded_port 2>/dev/null)" || {
+    print -u2 -- "Could not read the forwarded port from container: $container"
+    return 1
+  }
+
+  [[ -n "$port" ]] || {
+    print -u2 -- "Gluetun has not published a forwarded port yet."
+    return 1
+  }
+
+  print -r -- "$port"
 }
diff -ruN /mnt/data/zsh-original-layout/zsh/init/00-unalias-safe.zsh /mnt/data/zsh-reviewed/zsh/init/00-unalias-safe.zsh
--- /mnt/data/zsh-original-layout/zsh/init/00-unalias-safe.zsh	2026-07-29 11:32:58.722280621 +0000
+++ /mnt/data/zsh-reviewed/zsh/init/00-unalias-safe.zsh	2026-07-29 11:32:58.710280620 +0000
@@ -1,9 +1,10 @@
-# Clear common aliases before loading anything else.
-# Ignore missing aliases so interactive shells do not spam errors.
-unalias cat bat grep ls l ll la lla l1 l1a lt lt1 lt2 lt3 lta ld ltd \
+# Clear aliases that this configuration owns. This is mainly useful when
+# reloading ~/.zshrc in an already-running shell.
+unalias cat bat grep ls l ll la lla l1 lt lt1 lt2 lt3 lta \
   lnew lold lbig lsmall lext lver lgit lgits lperm linode llinks lbytes \
-  ldirsize labsdate lplain lstree lstack lmedia llogs lcompose lbroken \
-  lempty lchanged lworld dfh du1 cls reload please .. ... .... ..... \
-  composefiles srv server comics home media df du update upgradable ports \
-  myip weather moshu-ipscan tcat dps dcps dcu dcd dcl ddf dtop jerr jf \
-  2>/dev/null
+  ldirsize labsdate lplain df du dfh cls reload \
+  .. ... .... ..... composefiles srv server comics home media \
+  update upgradable ports myip weather moshu-ipscan tcat \
+  dps dcps dcu dcd dcl dlogs dexec ddf dtop \
+  docker-up docker-down docker-restart docker-logs docker-exec docker-upgrade \
+  jerr jf ffport 2>/dev/null
diff -ruN /mnt/data/zsh-original-layout/zsh/init/01-ohmyzsh.zsh /mnt/data/zsh-reviewed/zsh/init/01-ohmyzsh.zsh
--- /mnt/data/zsh-original-layout/zsh/init/01-ohmyzsh.zsh	2026-07-29 11:32:58.722280621 +0000
+++ /mnt/data/zsh-reviewed/zsh/init/01-ohmyzsh.zsh	2026-07-29 11:32:58.710280620 +0000
@@ -14,10 +14,14 @@
   command-not-found
   ssh-agent
   systemd
-  sudo
   zsh-interactive-cd
   zsh-syntax-highlighting
 )
 
-source "$ZSH/oh-my-zsh.sh"
-[[ -f ~/.p10k.zsh ]] && source ~/.p10k.zsh
+if [[ -r "$ZSH/oh-my-zsh.sh" ]]; then
+  source "$ZSH/oh-my-zsh.sh"
+else
+  print -u2 -- "Warning: Oh My Zsh not found at $ZSH"
+fi
+
+[[ -r "$HOME/.p10k.zsh" ]] && source "$HOME/.p10k.zsh"
diff -ruN /mnt/data/zsh-original-layout/zsh/init/10-shell.zsh /mnt/data/zsh-reviewed/zsh/init/10-shell.zsh
--- /mnt/data/zsh-original-layout/zsh/init/10-shell.zsh	2026-07-29 11:32:58.722280621 +0000
+++ /mnt/data/zsh-reviewed/zsh/init/10-shell.zsh	2026-07-29 11:32:58.710280620 +0000
@@ -1,10 +1,18 @@
 setopt autocd
 setopt correct
+setopt interactive_comments
+setopt auto_pushd
+setopt pushd_ignore_dups
+setopt pushd_silent
 
-HISTSIZE=20000
-SAVEHIST=20000
-HISTFILE=~/.zsh_history
+HISTSIZE=50000
+SAVEHIST=50000
+HISTFILE="$HOME/.zsh_history"
 
+setopt extended_history
 setopt share_history
-setopt hist_ignore_dups
+setopt hist_expire_dups_first
+setopt hist_find_no_dups
+setopt hist_ignore_all_dups
 setopt hist_reduce_blanks
+setopt hist_save_no_dups
diff -ruN /mnt/data/zsh-original-layout/zsh/init/20-aliases.zsh /mnt/data/zsh-reviewed/zsh/init/20-aliases.zsh
--- /mnt/data/zsh-original-layout/zsh/init/20-aliases.zsh	2026-07-29 11:32:58.722280621 +0000
+++ /mnt/data/zsh-reviewed/zsh/init/20-aliases.zsh	2026-07-29 11:32:58.710280620 +0000
@@ -1 +1,2 @@
-source ~/.dotfiles/zsh/.zshrc_aliases
+[[ -r "$HOME/.dotfiles/zsh/.zshrc_aliases" ]] && \
+  source "$HOME/.dotfiles/zsh/.zshrc_aliases"
diff -ruN /mnt/data/zsh-original-layout/zsh/init/30-fzf.zsh /mnt/data/zsh-reviewed/zsh/init/30-fzf.zsh
--- /mnt/data/zsh-original-layout/zsh/init/30-fzf.zsh	2026-07-29 11:32:58.722280621 +0000
+++ /mnt/data/zsh-reviewed/zsh/init/30-fzf.zsh	2026-07-29 11:32:58.710280620 +0000
@@ -1 +1,2 @@
-export FZF_DEFAULT_OPTS="--height 40% --layout=reverse --border"
+# Preserve options inherited from the environment or another plugin.
+export FZF_DEFAULT_OPTS="${FZF_DEFAULT_OPTS:+$FZF_DEFAULT_OPTS }--height 40% --layout=reverse --border"
diff -ruN /mnt/data/zsh-original-layout/zsh/init/50-path.zsh /mnt/data/zsh-reviewed/zsh/init/50-path.zsh
--- /mnt/data/zsh-original-layout/zsh/init/50-path.zsh	2026-07-29 11:32:58.722280621 +0000
+++ /mnt/data/zsh-reviewed/zsh/init/50-path.zsh	2026-07-29 11:32:58.710280620 +0000
@@ -1,9 +1,7 @@
-# Created by `pipx` on 2026-03-03 17:34:49
-export PATH="$PATH:/home/gari/.local/bin"
+# Zsh keeps $path and $PATH synchronized. -U removes duplicate entries.
+typeset -U path PATH
 
-# opencode
-export PATH=/home/gari/.opencode/bin:$PATH
+[[ -d "$HOME/.opencode/bin" ]] && path=("$HOME/.opencode/bin" $path)
+[[ -d "$HOME/.local/bin" ]] && path=("$HOME/.local/bin" $path)
 
-# >>> Codex installer >>>
-export PATH="/home/gari/.local/bin:$PATH"
-# <<< Codex installer <<<
+export PATH
